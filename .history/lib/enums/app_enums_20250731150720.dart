enum LoadingState {
  initial,
  loading,
  loaded,
  error,
}

enum AuthState {
  initial,
  authenticated,
  unauthenticated,
}

enum ThemeMode {
  light,
  dark,
  system,
}

enum ApiStatus {
  success,
  error,
  loading,
} 